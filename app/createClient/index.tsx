import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

const createClient = (props: Props) => {
  return (
    <View>
      <Text>createClient Page - WIP</Text>
    </View>
  )
}

export default createClient

const styles = StyleSheet.create({})